@{
    // El host puede sobreescribir este layout definiendo "_PhipesBlogLayout" en su propio
    // Pages/Shared (la resolución de vistas del host gana sobre la RCL).
    Layout = "_PhipesBlogLayout";
}
